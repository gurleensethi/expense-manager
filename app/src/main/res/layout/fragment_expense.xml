<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/enpense_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/window_expense_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background_color"/>

    <LinearLayout
        android:id="@+id/expense_navbar_linear_layout"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/dark_grey"
        android:orientation="vertical">

        <include layout="@layout/navbar_user_image"/>

        <ListView
            android:id="@+id/expense_navbar_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/dark_grey"
            android:divider="@android:color/white"
            android:backgroundTint="@color/dark_grey"/>

    </LinearLayout>

</android.support.v4.widget.DrawerLayout>